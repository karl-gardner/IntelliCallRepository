<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | IntelliCall</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <%- include('../partials/navbar', { page: 'dashboard', user: user }) %>

  <div class="container mt-5">
    <div class="row">
      <div class="col-12">
        <h1 class="mb-4">Welcome, <%= user.name %>!</h1>

        <div id="messageAlert" class="alert d-none" role="alert"></div>

        <div class="row">
          <div class="col-md-6 mb-4">
            <div class="card">
              <div class="card-header">
                <h5 class="mb-0">Dashboard Notes</h5>
              </div>
              <div class="card-body">
                <textarea
                  class="form-control"
                  rows="10"
                  id="textContent"
                  placeholder="Enter your notes here..."><%= dashboardData.textContent || '' %></textarea>
                <button
                  class="btn btn-primary mt-3"
                  id="saveBtn">
                  Save Data
                </button>
              </div>
            </div>
          </div>

          <div class="col-md-6 mb-4">
            <div class="card">
              <div class="card-header">
                <h5 class="mb-0">Saved Content</h5>
              </div>
              <div class="card-body">
                <div id="savedContent">
                  <% if (dashboardData.textContent) { %>
                    <div class="saved-content-display mb-3">
                      <pre><%= dashboardData.textContent %></pre>
                    </div>
                    <% if (dashboardData.updatedAt) { %>
                      <p class="text-muted small">
                        Last updated: <%= new Date(dashboardData.updatedAt).toLocaleString() %>
                      </p>
                    <% } %>
                  <% } else { %>
                    <p class="text-muted">No saved content yet.</p>
                  <% } %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/dashboard.js"></script>
</body>
</html>
