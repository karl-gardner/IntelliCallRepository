name: Build and Deploy Node App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout the repo
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: npm install

      # 4️⃣ Build project (TypeScript + Vite + copy assets)
      - name: Build project
        run: npm run build

      # 5️⃣ Create deployment ZIP
      - name: Create deployment zip
        run: zip -r release.zip dist node_modules package.json package-lock.json

      # 6️⃣ Upload ZIP as artifact (optional, for inspection)
      - name: Upload release.zip artifact
        uses: actions/upload-artifact@v3
        with:
          name: release-zip
          path: release.zip

      # 7️⃣ Login to Azure
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # 8️⃣ Deploy ZIP to Azure Web App
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}
          package: release.zip
